import styled from '@emotion/styled';

export const StyledSlyder = styled.section`
    box-sizing: border-box;
`;
export const StyledArrow = styled.div`
    :before {
        font-size: 26.5px;
        color: black;
        opacity: 0.5;
        :hover {
            opacity: .9;
        }
    }
`;